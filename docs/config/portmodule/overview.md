---
title: PortModules
---

PortModules can be created using the [`addPortModule`](../component/addPortModule) function to attach the PortModule to a specific component's port. The returned PortModule can then be further configured using the functions in this section.

